# TaxMind Pro Web

当前 Web 只提供第一批应用壳、内部专业辅助提示和 API 健康状态展示，不包含登录、事项、知识或审核业务。

```powershell
pnpm install
pnpm --filter @taxmind/web dev
pnpm --filter @taxmind/web test
pnpm --filter @taxmind/web build
```