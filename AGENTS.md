# TaxMind Pro 开发与协作规范

> **适用范围**：本文件位于仓库根目录，适用于 TaxMind Pro 的所有开发、测试、文档、配置、数据库迁移、Git 提交与 GitHub 协作活动。  
> **目标**：在不牺牲 MVP 交付速度的前提下，确保系统具备可追溯性、可测试性、可审计性和可回滚性。  
> **仓库**：`https://github.com/yikong666/TaxMind_pro.git`  
> **项目定位**：面向税务师事务所、代理记账机构和财税咨询机构的内部专业辅助系统；不是面向终端纳税人的自动报税系统。

---

## 0. 规范优先级与必须先读的文档

发生冲突时，按以下优先级执行：

1. 用户当前明确指令；
2. 本文件 `AGENTS.md`；
3. `TaxMind-Pro-Development-Design.md`（技术开发设计）；
4. `TaxMind-Pro-Project-Scaffold-Spec.md`（目录、文件与接口契约）；
5. `TaxMind-Pro-MVP-PRD.md`（产品范围与验收目标）；
6. 代码注释、历史实现和个人习惯。

每次开始任务前，必须先阅读本文件；涉及具体模块时，再读取上述对应设计文档和相关模块 README。**不得仅根据相似项目经验改写本项目的领域边界。**

### 0.1 已确定且不得擅自改变的项目边界

- 系统直接用户是财税机构成员；小规模纳税人、小型微利企业和个体工商户是被服务对象，不是系统账号用户。
- MVP 仅覆盖全国政策 + 广东省 + 深圳市口径下的有限税务事项；不宣称覆盖全中国税法。
- 仅使用官方公开资料、虚构、匿名化或经授权的数据；禁止接入电子税务局、真实企业账簿、全量发票明细和非公开申报数据。
- 系统只生成内部分析与客户答复草稿；不得自动申报、缴税、代替税务师做最终决定或直接对外出具正式税务意见。
- 风险命中和风险等级必须由确定性规则决定；LLM 只可用于理解、抽取、解释和草稿生成，**不得新增、删除或篡改规则结论**。
- 正式知识、FAQ、风险规则、办税事项、图谱关系都必须保留来源和审核状态；未审核内容不得进入正式检索与对外草稿依据。

---

## 1. 开发总流程：先理解，再方案，后实现

收到任何开发需求后，必须按以下顺序执行：

1. **理解需求**：明确用户目标、涉及的业务模块、数据影响和安全边界。
2. **检查现状**：先执行只读检查，包括 `git status`、相关代码、测试、配置和文档；不得盲改。
3. **输出简短方案**：说明准备改什么、不会改什么、涉及哪些文件、如何验证。
4. **等待用户确认**：除非用户明确说“直接实现 / 直接修改 / 开始开发”，否则不得开始写代码、改配置、创建迁移或提交 Git。
5. **最小范围实现**：只修改完成当前需求所必需的文件；不夹带重构、依赖升级或无关格式化。
6. **自测与复查**：运行对应测试、静态检查和必要的浏览器验证；阅读 `git diff` 确认没有无关改动。
7. **如实交付**：说明改动、验证结果、已知限制和下一步；测试失败时不得称为“完成”。

### 1.1 必须澄清的情况

出现以下任一情形，必须先提问，不得自行猜测：

- 需求存在多种业务含义，且会影响税务判断、数据模型、权限或审计；
- 缺少关键输入（如业务发生日、地区、纳税人类型、事实来源、验收标准）；
- 请求会扩大 MVP 范围，或需要接入真实客户数据、第三方账号、真实模型密钥；
- 需要删除数据、重置数据库、覆盖迁移、推送远程分支、合并 PR 或变更生产配置；
- 上游文档相互矛盾，且无法用优先级直接解决。

### 1.2 允许直接处理的低风险事项

用户明确要求直接执行时，可直接完成下列低风险、可回滚动作，但仍须测试和汇报：

- 补充或修正当前任务所需的单元测试、文档、Mock 数据；
- 修复已定位的 Bug；
- 实现用户已确认方案中的页面、接口、服务或配置；
- 执行只读检查、格式化、lint、类型检查和测试。

---

## 2. 架构与代码组织约束

项目采用 **Monorepo + 模块化单体 + 异步 Worker**。默认目录与依赖方向以 `TaxMind-Pro-Project-Scaffold-Spec.md` 为准。

### 2.1 技术基线

| 层次 | 基线 | 使用原则 |
|---|---|---|
| 前端 | React + TypeScript + Vite + Ant Design | 工作台 SPA；不引入第二套 UI 框架 |
| 前端状态 | TanStack Query + Zustand | 服务端状态与短期 UI 状态分离 |
| 后端 | Python 3.12 + FastAPI + Pydantic v2 | 路由只做协议转换，不承载业务规则 |
| 持久化 | MySQL 8 + SQLAlchemy 2 + Alembic | MySQL 是事务、审核、版本与审计的主数据源 |
| 异步 | Celery + Redis | 采集、解析、嵌入、图同步、评测不得依赖 Web 进程后台任务 |
| 知识检索 | Milvus + Neo4j + MySQL 精确过滤 | Milvus、Neo4j 都是可重建投影，不是业务主数据源 |
| 文件 | MinIO | 原始 HTML/PDF/附件/OCR/解析快照；数据库只存对象键与元数据 |
| 编排 | LangGraph | 仅用于多步查询、追问暂停/恢复和人工介入；简单查询不强行走图 |
| 部署 | Docker Compose | 仅作为开发与演示基线；不承诺生产高可用 |

### 2.2 严格依赖方向

```text
entrypoints (API / worker / scheduler)
        ↓
application services / use cases
        ↓
domain models + ports / contracts
        ↓
repositories / adapters / infrastructure
```

- `router.py` 只能调用 Service / Use Case，不能直接拼 ORM 查询、调用 Milvus/Neo4j 或编写业务判断。
- `service.py` 可以依赖 Repository、Port、领域对象和事务边界；不能依赖 FastAPI `Request`、`Response`。
- `repository.py` 只处理存取与查询；不得混入路由逻辑、LLM Prompt 或业务状态流转。
- 外部模型、向量库、图数据库、对象存储必须经 Adapter / Port 隔离；不得在领域层直接 import 具体 SDK。
- 前端页面不得绕过 API 直接访问 MySQL、Milvus、Neo4j、Redis 或 MinIO。
- 禁止跨模块直接读写对方 ORM Model；跨模块使用公开 Service、Contract 或领域事件。

### 2.3 数据与版本规则

1. 所有租户业务表必须有 `org_id`，所有查询必须自动注入机构上下文；公共政策数据不可被机构直接修改。
2. 政策文件、条款、FAQ、规则、办税事项、图谱关系和客户事实都要有版本、状态、创建人、更新时间和审计记录。
3. 已发布知识不得原地覆盖；修改必须生成新版本，经过审核后发布到新的知识快照。
4. API 不得对 MySQL、Milvus、Neo4j 做“尽力双写”。先提交 MySQL 主数据和 Outbox，再由 Worker 幂等投影。
5. 所有正式图谱节点、边和重要属性必须带可回溯来源：至少包含 `source_doc_id`、`source_chunk_id`、`source_url`、`effective_start`、`effective_end`、`region`、`status`、`extraction_confidence`、`review_status`。
6. Redis 只存短期会话状态、运行检查点、缓存、锁和队列消息；它不是政策、审核或长期会话事实的唯一存储。
7. FAQ 是审核发布的知识资产，不是会话记忆；不得将用户对话、模型临时输出自动写入 FAQ 或公共图谱。

---

## 3. 前端开发规范

### 3.1 页面与交互边界

MVP 前端入口固定为：

1. 事项工作台；
2. 政策检索与证据详情；
3. 办税事项库；
4. 审核中心；
5. 知识运营（含 FAQ）；
6. 反馈与审计。

对话流仅承载：智能追问、政策检索触发、证据卡、适用性、风险审查、办税指引和答复草稿。机构配置、知识批量治理、正式审核和审计必须保留独立页面，**不得为了“AI 感”将所有功能塞入聊天窗口。**

### 3.2 前端实现规则

- TypeScript 必须开启严格模式；禁止 `any`，无法建模时使用 `unknown` 并做类型收窄。
- 所有请求由统一 API Client 发起；接口类型来自 OpenAPI / 共享 Contract，不手写重复 DTO。
- 查询数据使用 TanStack Query；仅 UI 临时状态（抽屉、筛选、局部草稿等）使用 Zustand 或组件状态。
- 所有加载、空态、权限不足、网络异常、提交中、可重试失败都必须有可理解的界面状态。
- 高风险结果必须显式展示：证据来源、政策状态、业务日期、地区、信息缺口、置信/确定性标签、人工审核状态和“内部专业辅助”提示。
- 用户输入的客户事实只显示最小必要信息；Demo、截图与测试数据必须使用虚构或匿名化数据。
- 不把政策有效性、风险等级和审核状态只作为颜色或前端计算；前端只呈现后端返回的权威状态。
- 不得在浏览器保存模型密钥、数据库密码、真实客户资料或完整原始政策文件。

### 3.3 前端验收最低要求

每次新增或修改页面，至少覆盖：

- 正常数据展示；
- 加载中、空数据和错误重试；
- 权限不足或审核中状态；
- 关键操作的确认、成功/失败反馈；
- 1440px 桌面工作台布局；
- 关键流程可由浏览器自动化或人工浏览器验证。

---

## 4. 后端、知识与安全开发规范

### 4.1 API 与服务

- REST API 使用 `/api/v1` 前缀；SSE 只用于服务端单向的运行状态和回答增量。
- 写操作应提供幂等键或明确的乐观锁策略；可编辑聚合根使用 `version_no` 或 `If-Match`。
- 统一错误结构、错误码、请求 ID 和审计事件；禁止向客户端暴露堆栈、密钥、SQL 或模型服务内部错误。
- Pydantic Schema 是 API 边界；不得直接返回 ORM 对象、数据库 JSON 原样内容或第三方 SDK 响应。
- 所有时间统一以 UTC 存储和传输；金额使用 `DECIMAL`，禁止浮点数；地区使用 GB/T 2260 代码。

### 4.2 GraphRAG 与规则安全线

- 查询运行必须先执行范围闸门：业务发生日、地区、主体类型、纳税人类型、审核状态和知识快照。
- 普通文号/关键词查询不强行走图谱；只有涉及政策沿革、条件-义务-风险多跳关联时才进行受控图扩展。
- 证据融合后才可生成草稿；生成内容中的关键结论必须绑定已发布证据，引用校验失败必须降级为追问、转人工或不确定结论。
- 不得编造政策文号、条款、官方来源、处罚依据或地区口径；不确定时明确标记并要求人工核验。
- Prompt、模型名、模型参数、知识快照、检索候选摘要、规则版本和引用结果必须随 `run_id` 留档；不得记录模型私有思维链。
- LLM 输出、抽取关系和 OCR 结果默认是候选，不是正式知识；必须经过规则校验和人工审核才能发布。

### 4.3 数据采集与合规

- 仅采集白名单内、无需登录的政府官方公开内容；先人工筛选种子数据，再低频增量更新。
- 禁止绕过验证码、登录、签名、反爬机制，禁止代理池规避限制，禁止抓取电子税务局内部数据。
- 每个来源保存 URL、发布机关、文号（如有）、采集时间、内容哈希、版权/使用备注和解析状态。
- 原始资料只供内部解析、溯源和合规使用；不得实现对外原始资料整库下载。
- 对扫描 PDF 仅在确有必要时 OCR；原始文件、OCR 文本和结构化解析结果必须可关联、可复核。

---

## 5. 测试与质量门禁

### 5.1 测试分层

| 类型 | 目录/工具 | 必须覆盖 |
|---|---|---|
| 后端单元测试 | `apps/backend/tests/unit/` + pytest | 领域规则、Service、解析、路由决策、权限、异常边界 |
| 后端集成测试 | `apps/backend/tests/integration/` + pytest | Repository、迁移、Outbox、Redis/MinIO/Milvus/Neo4j Adapter |
| 契约测试 | `contracts/` + OpenAPI | 前后端请求/响应、错误码、SSE 事件结构 |
| 前端单元测试 | `apps/web/src/**/__tests__/` + Vitest | 组件状态、格式化、表单、权限展示 |
| 前端交互测试 | React Testing Library | 对话追问、证据抽屉、审核提交、错误重试 |
| E2E / 浏览器 | Playwright 或浏览器自动化 | 事项创建 → 分析 → 证据查看 → 提交审核等关键链路 |
| 金标准回归 | `evals/golden/` | 政策时效、地域过滤、风险规则、引用完整性、拒答/追问 |

### 5.2 新功能的最低要求

每一项新功能或 Bug 修复必须同步补充测试：

- 至少覆盖正常路径和一个关键边界/失败路径；
- 规则变更必须补充规则输入、预期命中和预期不命中的回归样例；
- API 变更必须更新 OpenAPI/共享契约，并检查前端调用；
- 数据库模型变更必须提供 Alembic migration 和升级/降级验证；
- UI 变更必须有浏览器验证；涉及关键流程时要有 E2E 覆盖。

### 5.3 提交前必须执行的检查

根据改动范围执行，所有涉及项必须通过：

```bash
# 根目录：基础检查（实际命令以 Makefile 为准）
git status --short
git diff --check

# 后端
cd apps/backend
uv run ruff check .
uv run ruff format --check .
uv run mypy src tests
uv run pytest tests -v

# 前端
cd apps/web
pnpm lint
pnpm typecheck
pnpm test --run
pnpm build

# 有页面或交互改动时
pnpm test:e2e
```

规则：

- 测试失败、类型检查失败、构建失败或 E2E 关键路径失败时，不得通知用户“可以验收”。
- 外部服务不可用导致无法运行的检查，必须明确写出“未执行 / 失败原因 / 风险”，不得伪造通过结果。
- 前端改动完成后，必须用浏览器验证页面可启动、关键交互可用、控制台无阻塞错误，再提醒用户查看。
- 不为让测试通过而降低断言、跳过测试、删除失败用例或使用无意义 Mock；确需调整测试预期时要说明业务原因。

---

## 6. 日志、审计与问题排查

### 6.1 日志的双通道要求

后端运行日志必须同时输出到：

1. **控制台**：本地开发和容器日志收集使用；
2. **文件**：写入 `var/log/taxmind/`，采用按天或按大小滚动、保留周期和权限控制。

`var/log/` 必须被 `.gitignore` 忽略；生产环境可由容器 stdout 收集平台替代本地文件，但不能取消结构化运行日志。

### 6.2 日志格式与必填字段

开发环境可使用可读文本，测试/生产使用 JSON 结构化日志。每条业务/错误日志至少带有：

| 字段 | 说明 |
|---|---|
| `timestamp` | UTC ISO 8601 时间 |
| `level` | `DEBUG` / `INFO` / `WARNING` / `ERROR` |
| `service` | `api` / `worker` / `scheduler` / `web` |
| `event` | 稳定、可检索的事件名，例如 `query.run.started` |
| `request_id` | HTTP 请求链路 ID；异步任务使用 `task_id` |
| `run_id` / `case_id` | 有则写入；不记录无关客户内容 |
| `org_id` | 机构上下文；公共知识任务可为空 |
| `actor_id` | 操作人；系统任务使用服务身份 |
| `duration_ms` | 完成/失败事件必须写入 |
| `error_code` | 错误时必须写入稳定业务错误码 |

### 6.3 日志级别与禁止项

- `DEBUG`：本地诊断细节，默认生产关闭；
- `INFO`：生命周期、任务开始/完成、状态流转、发布、审核、外部调用摘要；
- `WARNING`：可恢复异常、低置信抽取、降级、重试、数据缺口；
- `ERROR`：不可恢复异常、任务最终失败、数据一致性问题、安全事件。

严禁写入日志、异常文本、前端埋点或 Git 历史的内容：

- 密码、Token、Cookie、密钥、数据库 DSN 中的密码；
- 真实纳税人姓名、身份证号、统一社会信用代码、手机号、地址、账号、完整账簿/发票明细；
- 完整 Prompt、原始政策全文、模型私有思维链；
- 未脱敏的用户输入与模型输出。

需要排障时，只记录脱敏摘要、长度、哈希、字段名、来源 ID 和可控采样的片段。新增日志必须帮助定位问题，禁止为“显得有日志”而逐行打印无意义内容。

### 6.4 运行日志与审计日志必须分离

| 类型 | 用途 | 是否可删除 | 典型内容 |
|---|---|---|---|
| 运行日志 | 排障、性能、任务重试 | 按保留策略滚动 | 调用耗时、异常码、队列任务状态 |
| 审计日志 | 责任回放、质检、合规 | 不允许普通业务删除 | 谁在何时基于哪个事实/证据/规则版本进行了何种动作 |

审计日志必须记录：客户事实版本、知识快照、政策/证据版本、规则版本、模型与 Prompt 版本、审核决定、人工修改、发布/撤回和反馈处理。审计日志中同样只保存最小必要的脱敏信息。

### 6.5 问题排查顺序

发生问题时，按以下顺序处理：

1. 收集 `request_id`、`task_id`、`run_id`、时间范围和复现步骤；
2. 从运行日志定位模块、错误码和失败阶段；
3. 判断是输入缺失、权限/租户、数据状态、依赖服务、规则、检索投影还是生成质量问题；
4. 使用最小虚构/脱敏样本复现；
5. 修复根因并补充回归测试；
6. 复跑测试和必要的 E2E；
7. 在 PR/任务记录中写明根因、影响、修复与验证结果。

禁止只靠吞异常、无限重试、前端隐藏错误、重启容器或直接修改线上数据来“修复”。

---

## 7. Git 与 GitHub 协作规范

### 7.1 仓库与远程约定

初始化后必须确认远程地址：

```bash
git remote -v
# 期望 origin 为：
# https://github.com/yikong666/TaxMind_pro.git
```

- 默认主分支为 `main`；`main` 始终应处于可构建、可测试、可演示状态。
- 禁止直接在 `main` 上进行日常开发、强推、重写历史或使用 `git reset --hard` 清除未确认改动。
- 每次开始前先执行 `git status --short`；工作区已有无关改动时必须保留，不得覆盖或随手纳入提交。
- 禁止提交 `.env`、真实日志、数据目录、模型权重、缓存、临时导出、截图中的敏感数据和任何密钥。

### 7.2 分支命名

从最新 `main` 创建短生命周期分支：

```text
feat/<module>-<short-description>
fix/<module>-<short-description>
docs/<short-description>
test/<module>-<short-description>
refactor/<module>-<short-description>
chore/<short-description>
```

示例：

```text
feat/cases-workspace
feat/retrieval-evidence-card
fix/policy-effective-date-filter
docs/development-governance
test/risk-rule-regression
```

一个分支只解决一个可审核目标；跨模块大需求应拆为多个可独立验证的 PR，而不是累积成巨型提交。

### 7.3 提交规范

使用 Conventional Commits，格式如下：

```text
<type>(<scope>): <中文简短说明>
```

允许的 `type`：

| 类型 | 适用场景 |
|---|---|
| `feat` | 新能力 |
| `fix` | Bug 修复 |
| `docs` | 文档变更 |
| `test` | 测试新增或修正 |
| `refactor` | 不改变外部行为的重构 |
| `perf` | 性能优化 |
| `build` | 构建、依赖、Docker、CI |
| `chore` | 其他维护事项 |

示例：

```text
feat(cases): 新增事项最小画像与事实版本
fix(retrieval): 按业务发生日过滤已失效政策
test(risk): 覆盖专票相关风险规则边界
docs(governance): 补充知识发布审核流程
```

提交要求：

- 一个提交只包含一个逻辑目的；代码、必要测试和对应文档可以同一提交；
- 提交前必须检查 `git diff --check` 和 `git diff --staged`；
- 禁止使用 `git add .` / `git add -A` 盲目暂存；逐项 `git add <path>`；
- 禁止 `--no-verify` 跳过 Hook；
- Commit message 不得包含密钥、真实客户信息、政策原文大段内容或模型输出。

### 7.4 Push、PR 与合并

- 本地 `commit`、`push`、创建 PR、合并 PR 都属于独立动作：未获得用户明确授权时，Codex 只完成本地修改与测试，不得自行推送或创建 PR。
- 用户授权推送后，先确认当前分支、目标远程、暂存内容和测试通过状态，再执行 `git push -u origin <branch>`。
- PR 目标默认是 `main`，标题使用与提交一致的 Conventional Commit 格式。
- PR 描述必须包含：背景/目标、主要改动、明确不包含的内容、测试命令及结果、数据库迁移/配置影响、风险与回滚方式、截图或录屏（有 UI 改动时）。
- 合并前必须通过 CI、必需测试和人工 review；MVP 推荐采用 **Squash and merge** 保持 `main` 历史清晰。
- 禁止直接向 `main` force push，禁止未经确认自行关闭、合并或删除他人 PR/分支。

### 7.5 GitHub Issue、PR 与里程碑建议

建议在 GitHub 使用以下标签：

```text
type:feature, type:bug, type:docs, type:test, type:chore
area:web, area:api, area:knowledge, area:retrieval, area:risk, area:infra
priority:p0, priority:p1, priority:p2
status:blocked, status:needs-review
```

每个 Issue 写清：业务背景、范围、验收标准、排除项、依赖、风险和测试要求。关联 PR 使用 `Closes #<issue-number>`；不要用提交标题替代 Issue 描述。

---

## 8. 配置、依赖、迁移与环境规范

### 8.1 配置与密钥

- 只提交 `.env.example`，不得提交真实 `.env`；任何疑似密钥一旦进入 Git 历史，立即停止传播、撤销密钥、联系项目负责人处理。
- 所有环境变量由 Settings 统一校验和加载；禁止在业务代码中散落 `os.getenv()`。
- Dev/Test/Prod 必须区分配置；默认开发环境使用 Mock/本地依赖，禁止把真实生产地址设为默认值。
- 模型名称、超时、重试、索引版本、知识快照等可变参数必须配置化并在运行审计中留档。

### 8.2 依赖管理

- Python 依赖只通过 `uv` 和 `pyproject.toml` 管理，生成的 `uv.lock` 必须提交。
- 前端依赖只通过 `pnpm` 管理，生成的 `pnpm-lock.yaml` 必须提交。
- 新增依赖前必须说明它解决的真实问题、许可证/安全风险、替代方案和是否与已有组件重叠。
- 禁止随意升级大版本、混用 npm/yarn/pnpm、混用 pip/poetry/uv，或为单一小功能引入庞大框架。

### 8.3 数据库与索引变更

- 所有 MySQL Schema 变更必须通过 Alembic Migration；禁止手工改库后只修改 ORM Model。
- Migration 文件必须说明升级、降级、数据兼容性和大表/锁风险；首次执行必须在空库和测试数据上验证。
- Milvus Collection、Neo4j Schema/索引、MinIO Bucket/生命周期规则的变化必须有可重复脚本或版本化定义。
- 变更嵌入模型、分块策略、稀疏检索字段或图谱本体时，必须创建新索引/快照版本，评测通过后切换；禁止原地混写不同版本向量。
- 数据修复脚本必须可重复执行、可审计，并先在虚构/脱敏环境验证。

---

## 9. 文档、变更记录与完成定义

### 9.1 必须维护的文档

| 文档 | 何时更新 |
|---|---|
| `README.md` | 启动命令、运行方式、最小演示路径、依赖发生变化时 |
| `AGENTS.md` | 开发约束、协作流程、质量门禁发生变化时 |
| `docs/CHANGELOG.md` | 面向项目成员的功能、修复、破坏性变更、迁移说明 |
| `docs/adr/` | 影响架构、存储、模型、检索、规则或安全边界的重要决策 |
| `docs/runbooks/` | 部署、故障排查、数据导入、索引重建、回滚等可操作流程 |
| OpenAPI / Contracts | API、SSE 事件或前端 DTO 发生变化时 |

`CHANGELOG` 应按日期/版本记录“变更内容、影响模块、迁移/回滚、验证结果”；不要把每次格式化都写成产品变更。

### 9.2 Definition of Done（完成标准）

任务只有同时满足下列条件，才可说明“可验收”：

1. 实现与已确认需求一致，且没有超出 MVP 边界；
2. 代码结构遵循模块边界，未引入无解释的依赖或重复实现；
3. 新功能/修复有对应测试，相关质量检查全部通过；
4. 页面改动已通过浏览器验证；
5. 关键日志、审计事件、错误处理和脱敏要求已落实；
6. API、迁移、配置、文档和 CHANGELOG 已在需要时同步更新；
7. `git diff` 已人工复核，无无关文件、密钥、真实数据或生成物；
8. 若用户要求 GitHub 协作：已在明确授权后完成正确的分支、提交、Push/PR，并报告链接或状态。

### 9.3 交付汇报模板

完成后使用简短、如实的格式汇报：

```markdown
## 完成内容
- …

## 验证结果
- `命令`：通过 / 未执行（原因）
- 浏览器验证：…

## 影响与注意事项
- 配置 / 迁移 / 数据 / 已知限制：…

## Git 状态
- 分支：…
- Commit：…（仅在已提交时）
- Push / PR：…（仅在用户已授权时）
```

---

## 10. 禁止清单

以下行为一律禁止，除非用户给出明确、具体的书面授权且风险已解释：

- 绕过官方站点登录、验证码、接口签名、反爬限制或使用代理池采集税务资料；
- 接入、提交、展示或记录真实客户敏感涉税数据；
- 自动申报、缴税、登录电子税务局或代表机构出具正式意见；
- 让 LLM 单独做风险定级、有效期判断、政策适用最终结论，或伪造来源；
- 未审核即发布知识、FAQ、规则、图谱关系或客户答复；
- 删除审计记录、重写已发布知识历史、破坏事实与证据的版本关联；
- 提交密钥、真实 `.env`、日志、数据库文件、模型权重和大规模爬取数据；
- 在有未确认改动时运行破坏性 Git 命令、强推远程、重写主分支历史；
- 为了通过测试而跳过、删除或弱化测试，以及伪造测试/浏览器验证结果。

---

## 11. 给 Codex 的执行提醒

TaxMind Pro 不是一个“把向量检索结果拼给大模型”的聊天 Demo。每一个开发决策都必须能回答三个问题：

1. 它解决的是财税机构的什么真实工作问题？
2. 它的结论、关系或数据能否回溯到事实、规则和官方来源？
3. 出错、不确定或信息不足时，系统是否会停止过度结论、明确提示并交给专业人员审核？

如果不能清楚回答，先停下来澄清或简化设计，不要为了堆技术组件而继续实现。
